#include <stdio.h>
/**
 * main - use putchar to print alphabet
 *
 * Description: print alphabets in lowercase.
 *
 * Return:0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
